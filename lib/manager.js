const Employee = require("./index");

class Manager extends Employee {
    constructor(id, number, email) {
        super(id, number, email);
        this.officeNumber = officeNumber
        this.role = role
    }
}

