const Employee = require("./index");

class Engineer extends Employee {
    constructor(id, name, email) {
        super(id, name, email);
        this.gitHub = gitHub
        this.role = role
    }
}